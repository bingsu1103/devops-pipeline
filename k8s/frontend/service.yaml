apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: devops-app
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000 # Thay đổi đồng bộ từ 80 -> 3000
      targetPort: 3000
  type: ClusterIP
